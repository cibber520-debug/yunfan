# 后端架构提示词体系交付概要

## 已完成

- 基于现有 React/TypeScript 前端契约与志愿推荐 PRD，生成一套可复用的生产级后端架构提示词体系。
- 包含：运行前输入清单、系统角色提示词、项目上下文、总控架构提示词、14 个专项深化/红队/收口提示词、执行顺序与合格线。
- 重点覆盖：3k/10k/30k 总 RPS 容量建模、模块化单体到服务化演进、缓存防雪崩、PostgreSQL 数据架构、不可变发布组合版本、推荐引擎、ETL 数据门禁、多可用区与容灾、安全隐私、可观测、压测和成本。

## 关键决策

- MVP 不直接堆微服务：优先“模块化单体 + 可独立扩容的推荐计算 + 独立数据管线”，达到量化阈值后再拆分。
- 高可用不能牺牲推荐正确性：所有推荐绑定 `release_bundle_id`，统一锁定数据、规则、算法、Schema 和快照哈希；数据不可信时使用最后可信版本或明确降级。
- 3k/10k/30k 是压测档位而非真实用户预测；总 RPS 必须拆到各接口，并以基准测试反推实例数。
- 用户私有画像、草稿与推荐运行元数据不得进入共享缓存；高基数 ID 仅进入日志/Trace。

## 使用建议

先把实际首发省份、峰值用户、接口流量占比、团队技术栈、云平台、预算和数据来源填入 `INPUT_MANIFEST`，再依次运行 Prompt 0 + Prompt 1 + Prompt 2。总体架构确认后，至少深化容量、数据库、缓存、推荐引擎和容灾，最后执行红队评审与最终决策包。

## 校验结果

文档结构检查通过：194 行，36 个代码围栏成对闭合，Prompt 0–16、输入清单、版本发布、容量与核心专项均存在。独立架构评审结论为“无 P0，有条件通过”，P1 建议已合并到最终文档。
